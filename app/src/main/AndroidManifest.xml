<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sp.familiavillage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivitiesHomePage"/>
        <activity android:name=".ActivitiesNaturePage"/>
        <activity android:name=".SettingsPage"/>
        <activity android:name=".QRCodePage"/>
        <activity android:name=".RewardsHomePage"/>
        <activity android:name=".RewardsFoodiePage"/>
        <activity android:name=".VillagePage"/>
    </application>

</manifest>